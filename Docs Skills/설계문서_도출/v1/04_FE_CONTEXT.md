# 04_FE_CONTEXT.md
# Frontend AI Agent — Context & Instruction

> **이 파일을 읽는 대상:** 프론트엔드 코드를 작성하는 AI Agent
>
> **읽는 방법:**
> 1. SECTION 0 (역할과 규칙) 를 먼저 읽고 숙지
> 2. SECTION 1~4 를 읽어 전체 맥락 파악
> 3. SECTION 5 (현재 태스크) 를 보고 작업 시작
> 4. 모르거나 결정이 필요하면 SECTION 6 (판단 기준) 를 참고
> 5. 작업 완료 후 SECTION 7 (완료 체크리스트) 확인

---

## SECTION 0 — 역할과 규칙

### 이 Agent의 역할
```
(예시)
- 웹 UI 컴포넌트 구현
- 사용자 흐름(페이지 전환, 상태 관리) 구현
- API 연동 (BE가 만든 엔드포인트 호출)
```

### 절대 하지 말 것
```
- BE 서버 코드 수정
- API 경로 / 요청·응답 형식 임의 변경
- DB 직접 접근
- 02_SPEC.md의 "Won't" 항목 구현
```

### 판단이 필요할 때
```
1. 먼저 이 파일 SECTION 3 (화면 명세) 확인
2. 거기도 없으면 → 가장 단순한 선택을 하고 코드에 TODO 주석
3. 사람에게 물어봐야 할 것은 응답 마지막에 [QUESTION] 으로 모아서 출력
```

---

## SECTION 1 — 프로젝트 맥락 요약

```
(02_SPEC.md 에서 복사)
- 프로젝트명:
- 목적:
- 대상 사용자:
- 실행 환경:
```

---

## SECTION 2 — FE 담당 기능

### 구현할 기능 (02_SPEC.md Must 중 FE 담당)
| # | 기능명 | 설명 | 우선순위 |
|---|--------|------|----------|
| | | | High / Mid / Low |

### 구현하지 않는 기능
```
- (02_SPEC.md Won't 항목 그대로)
```

---

## SECTION 3 — 화면 명세

> 각 화면(페이지 / 모달)의 역할과 구성 요소

### 화면 1: [화면 이름]

**목적:** (이 화면이 하는 일)

**진입 경로:** (어디서 이 화면으로 오는가)

**구성 요소:**
```
- [컴포넌트명]: 역할
- [컴포넌트명]: 역할
```

**사용자 액션 → 결과:**
| 액션 | 결과 |
|------|------|
| 버튼 클릭 | 다음 화면으로 이동 |
| 폼 제출 | POST /api/... 호출 후 결과 표시 |

**에러 케이스:**
```
- API 실패 시: 에러 메시지 표시
- (기타 케이스)
```

---

## SECTION 4 — API 호출 목록

> FE가 호출하는 BE API 목록. 03_BE_CONTEXT.md SECTION 4와 일치해야 함.

| 화면 | Method | Path | 언제 호출 |
|------|--------|------|----------|
| | GET | /api/... | 화면 진입 시 |
| | POST | /api/... | 폼 제출 시 |

---

## SECTION 5 — 현재 태스크

> 사람이 작업 시작 전에 이 칸을 채워서 Agent에게 넘김

### 지금 구현할 것
```
(예시)
1. 온보딩 화면 — 사이트 등록 폼 컴포넌트
2. 요소 클릭 → AI 추론 결과 표시 모달
```

### 참고할 기존 코드
```
(있으면 경로 명시)
- src/components/Button.tsx → 이 스타일로 통일
```

### 완료 기준
```
- 지정한 화면이 모두 렌더링됨
- API 호출 시 로딩 / 성공 / 에러 상태가 모두 처리됨
```

---

## SECTION 6 — 코딩 규칙

```
- 프레임워크:
- 스타일링:
- 상태 관리:
- API 클라이언트: (예: axios, fetch)
- 폴더 구조:
  src/
    pages/
    components/
    hooks/
    api/
- 컴포넌트 원칙: (예: props 타입은 반드시 정의, 서버 상태는 훅으로 분리)
- 접근성: (예: 버튼에 aria-label 필수)
```

---

## SECTION 7 — 완료 체크리스트

작업 완료 후 확인:
- [ ] 모든 화면이 SECTION 3 명세와 일치하는가?
- [ ] 로딩 / 성공 / 에러 상태가 모두 구현되어 있는가?
- [ ] API 경로가 SECTION 4 목록과 일치하는가?
- [ ] TODO 주석이 있으면 SECTION 0의 [QUESTION] 으로 올렸는가?
- [ ] 모바일 / 데스크탑 레이아웃이 깨지지 않는가?
