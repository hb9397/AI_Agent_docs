# [프로젝트명] [FE/BE/전체] — AI Guide

> [부제 또는 시스템 설명 한 줄]
> [기술 스택 태그 — 예: React + Vite + Tailwind CSS] 기반 Agent 참조용 문서

---

## 1. Context (프로젝트 맥락)

### 프로젝트 개요
<!-- 3~5줄. 무엇을 하는 시스템인지, FE/BE 역할, 통신 방식, 실행 환경 -->

### 기술 스택

| 분류 | 라이브러리 | 버전 |
|------|-----------|------|
| | | |

### 아키텍처

```
[프로젝트명]/
└── [레이어 폴더]/
    ├── [파일]     # 역할 설명
    └── [폴더]/
        └── [파일] # 역할 설명
```

### 페이지 / 화면별 역할
<!-- 라우터 경로 + 컴포넌트명 + 핵심 역할 -->

- **`[경로]` ([컴포넌트].jsx)**: [역할 설명]

### 백엔드 통신 규칙
<!-- API 호출 방식, WebSocket URL, Base URL, 환경변수 참조 방법 -->

### API 함수 목록
<!-- 모든 API 함수를 한 곳에서 관리하는 파일 기준으로 나열 -->

```javascript
[함수명]([파라미터])  // [HTTP 메서드] [경로] — [설명]
```

### WebSocket 메시지 타입

| type | 설명 |
|------|------|
| | |

### 핵심 컴포넌트 및 로직
<!-- 프로젝트 고유 핵심 컴포넌트만. 일반 UI 컴포넌트는 생략 -->

### 핵심 도메인 개념
<!-- 프로젝트 고유 용어, 핵심 식별자 정의 -->

### 디자인 시스템
<!-- 테마, 색상 변수, 전역 CSS 설정 등 -->

---

## 2. Instruction

코드 작성에 있어서는 늘 아래 지침들을 준수한다.
@.instruction/basic-instruction.md

---

## 3. 실행 방법 및 환경변수

### [배포 환경 — 예: Podman Compose / Docker]

```bash
# 실행 명령
# 접근 URL
```

### 로컬 직접 실행 (개발 환경)

```bash
# 초기 세팅 (최초 1회)

# 실행 명령
```

### 환경변수

#### `.env.development`

```bash
# 변수명=설명
```

#### `.env.production`

```bash
# 변수명=설명
```

---

## 4. 기타

### VSCode 익스텐션

| 익스텐션 | 용도 |
|----------|------|
| | |
